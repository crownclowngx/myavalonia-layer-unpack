# G0010 / R04 实施结果

日期：2026-09-08。状态：已实施／完整本地门禁通过；部分原生交互待验收，未发布。最终验证时间为 09:35（Asia/Shanghai），发布与实机遗留单独列出。

## 本期交付

ZIP/ZIP64 浏览、分页搜索、稳定身份、目录明确后代与三态选择、一层提取；原包摘要变化检测、重复路径和大小写冲突解释、共享 CRC/AES 校验、不覆盖事务、累计读取/展开预算、取消与关闭排空。

新增第三个普通浏览 Document、三 Scope Standalone，密码当前调用隔离，“全部解压…”转入真实原解压页并展示来源和层数。保持原解压无需先浏览的流程。

专项：[方案](implementation.md)、[浏览契约](browsing-contract.md)、[格式矩阵](format-support-matrix.md)、[交互](interaction-design.md)、[验收](acceptance-matrix.md)。

## 验证

执行 `./tools/verify-local.ps1`，全部步骤通过：

| 本地检查 | 实际结果 |
| --- | --- |
| locked restore | 通过；依赖与锁文件没有新增变动 |
| Debug 全解决方案 `-warnaserror` | 通过；零警告、零错误 |
| Debug 引用资产求值 | 通过；只核对真实引用与许可证，不生成正式插件包 |
| Headless 全量测试 | 230 项通过，零失败、零跳过；较 G0009 新增 48 项 |
| Plugin/UI 全量测试 | 62 项通过，零失败、零跳过；较 G0009 新增 14 项 |
| `dotnet format --verify-no-changes --no-restore` | 通过 |
| Markdown 路径检查 | 73 份文档通过；结果归档后再次检查 |
| `git diff --check` | 通过 |

合计 **292 项测试全部通过，新增 62 项**。覆盖真实普通/加密/ZIP64 内容、异常路径、来源替换、预算、取消、页面与 Scope，保留原解压/压缩全部回归。核对时补充了“准备全部解压期间取消必须转发给真实输入扫描”的回归。最初 AES 与旧编码测试夹具的设置问题已按上游公开密码和强制旧编码修正，没有删除或跳过测试。

环境：Windows `10.0.26200` / x64、.NET SDK `10.0.302`、Avalonia `12.1.0`、Plugin SDK `3.3.0`、SharpZipLib `1.4.2`、SharpCompress `0.50.4`。完整命令、耗时、环境与指标见[验证摘要](verification-summary.json)。

验证基线提交：`d2c7d74ba2591a9a288105f8e05f8ebc694621e5`；实际验证包含其后的工作区改动。139 个源码/测试/工具/配置文件的组合 SHA-256 为 `c0c2143c96fdabdbd3042b467634ee3f2418f706d76cb103bed2be0080e79d8e`；按序号排序的路径与原始文件 SHA-256 生成 UTF-8 清单再哈希，完整范围与算法写在摘要中，不包含 docs、根 README、bin/obj 或 artifacts。

原始日志、TRX 和文件摘要清单归档到 `artifacts/G0010/local-verification/`；16 张两主题、两尺寸、四状态渲染图位于 `artifacts/ui/G0010/`。实际检查了浅色 640×520 准备页、深色 640×520 缺密页和浅色 900×760 成功页，主要动作、密码输入和成功结果入口可见，窄窗口列表与详情可独立滚动。这些不是原生桌面截图或 DPI 验收。

65,536 条 ZIP64 基线：源文件 7,143,522 字节、65,537 显示行，目录打开约 450.96 ms、尾页查询约 2.63 ms，打开累计读取 21,431,155 字节；提取两个一字节文件后累计读取 50,005,414 字节，展开 2 字节，真实内容一致。测量范围累计分配 172,579,688 字节，测试进程峰值工作集 162,885,632 字节；这不是任意输入的内存或耗时承诺。原始指标位于 `artifacts/performance/G0010/zip64-directory.json`，并纳入验证摘要。

## 范围与遗留

新建浏览 Document 为本期入口，未新增输入/结果上下文菜单。SDK 公开接口没有任意路径新建导航，因此全部解压在当前页承载真实原任务；没有引用 Host 内部接口。

仅声明 ZIP 浏览与选择提取；其他格式和加密头明确引导到原解压任务。完整源摘要会读遍原包，实际计量，不承诺只读取选中文件的压缩字节。

原生选择器、真实拖放、125%/150%/200% DPI、辅助功能和真实 Host 待实机验收。未使用 AIFLOW，未运行 Windows CI、Release、正式插件 ZIP、部署安装或发布门禁。
