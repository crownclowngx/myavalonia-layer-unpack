# G0015 当前交付记录：方案已归档

日期：2026-09-19。状态：**已生成详细方案／功能待实施**。此文件目前记录文档交付，实施后再按实际证据补充，不能视为分卷解压已经完成。

## 本轮交付

针对已确认的“外层解开后，同一目录包含标准 7z 多卷”场景，推荐同目录逻辑归组、受控联合读取与按层串行调度。区分分卷读取缺口与 DFS 顺序，保留默认一层和按需递归，不增加 GUI 技术选项。

已归档：[实施计划](plan.md)、[技术方案与 SOLID 分工](implementation.md)、[卷组契约](split-volume-contract.md)、[调度专项](layered-scheduling-design.md)、[验收矩阵](acceptance-matrix.md)。包含 G0015-01–08 工作项、G15-AC01–30 验收场景、共享调用方兼容、中文注释要求、资源与恢复策略。

当前基线为 `a1e2a70fdb2e95a98814ada25858697feaa9dfab`。本轮只增加和同步 Markdown；未修改业务源码、依赖、测试、脚本或 CI。历史 G0001–G0014 的测试结果不用于本阶段证明。

## 本轮文档检查

实际检查结果：

| 检查 | 本轮结果 |
| --- | --- |
| `./tools/check-docs.ps1` | 107 份 Markdown 相对路径检查通过 |
| `git -c core.autocrlf=input -c core.safecrlf=false diff --check` | 已跟踪文档差异空白检查通过 |
| 新文件检查 | 6 份专项 Markdown 无行尾空白与冲突标记，均明确待实施／未执行状态 |
| 编号与映射 | 8 个工作项、30 个唯一验收编号核对通过；阶段索引、产品入口与 R07 映射一致 |
| 变更范围 | 6 份新增专项文档、7 份同步入口文档，共 13 份 Markdown；无业务源码、依赖、测试或脚本变更 |

功能测试、独立分卷互操作、资源实测与原生交互均未执行，也没有生成受测源码摘要或填写虚构测试数量。以上仅证明文档交付及其链接、范围一致性，不证明功能已经实现。

## 待实施和待确认

- 锁定版本分段读取在实际异步、取消、固实及加密组合下仍需真实样本验证。
- 用户实际总卷大小、卷数和最大展开文件尚未确认；计划保持当前 4 GiB 输入、2 GiB 单文件展开、10 GiB 批次展开默认值，输入改按整组核算。
- 按层队列、全部卷摘要、共享检查／转换／Workflow、恢复入口及对应测试尚未实现。
- 不使用 AIFLOW；Windows CI、Release、正式插件包、部署、安装和发布门禁未执行。

## 实施后必须补充的事实

实际修改、与方案的偏差、最终成员及重试契约、验证环境和命令、源摘要、测试与真实互操作、性能／句柄／取消数据、界面截图和实机结果。受限格式、未测体量、原生遗留和发布边界分别记录；只有实际完成适用验证后才能更新能力矩阵与阶段状态。
