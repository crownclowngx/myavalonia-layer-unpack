# G0009 / R03 验收矩阵

日期：2026-09-08。以下场景已具备实现与自动测试，最终执行数量和门禁状态见[结果](result.md)。

| 场景 | 证据 | 自动化出口 |
| --- | --- | --- |
| R03-AC01 多文件夹分别打包 | PackBatchTests 十文件夹真实 ZIP、中文后代、逐文件摘要 | 一组一包、映射明确 |
| R03-AC02 默认合包 | 合并/分别父子去重对比；原 PackTests 和 PackDocumentTests 回归 | 一个事务；普通操作无新增必选项 |
| R03-AC03 部分/全部排除 | 目录传播、大小写、后缀精确匹配、默认隐藏内容、原空目录、全部排空 | 可检查命中项；空组跳过；不静默筛选 |
| R03-AC04 一组读取失败 | 真实独占来源、准备时缺失、输出注入故障 | 兄弟组成功保留；诊断不含引擎原文 |
| R03-AC05 正确/错误密码 | PackEncryptionTests 八种映射、正确密码原始内容、无密码/错密码回读 | 原内容正确，错误密码无成功输出 |
| R03-AC06 认证损坏/跨工具 | 两套库的清单/长度/摘要；真实认证尾部翻转；外部 Stored AES 回归 | 认证异常被发现；版本和读取方式见矩阵；桌面工具人工项待验 |
| R03-AC07 重试/中途取消 | 读取恢复、超时恢复、取消当前和后续组、重复执行互斥、源变更拒绝、关闭排空 | 成功不重复创建；取消不当失败重试；未完成项可新任务 |
| R03-AC08 会话隔离 | 同批次明确同密码、不同随机盐、两个创建会话加解压会话、GUI 同类/异类页 | 参数密码不继承，结果/计划/GUI 序列化无秘密 |
| R03-AC09 字节统计 | 零字节文件、随机不可压缩内容、仅打包、批次成功项汇总 | 无除零和虚构节省；实际归档可比源大 |

补充门禁：非法规则和模式、批次累计字节/条目、批次准备取消/总超时、单包输出预算、AES 写入与收尾取消、请求/结果只读、源内分别输出拒绝、已有目标及提交竞态编号、UI Tab/密码绑定/深浅主题/大小窗口。

真实格式测试不能被替身替代。故障替身只控制执行时机，恢复路径仍调用真实 ZIP 写入；保留原 172 项回归。统一脚本对两个测试项目分别检查 TRX，非零、全通过、无跳过，另执行锁定恢复、Debug 零警告构建、真实引用资产、格式和文档路径。

未执行：原生选择器与拖放、真实 DPI、桌面接收工具、干净插件安装、真实 Host/ALC、Release、正式 ZIP、部署、Windows CI 和发布门禁。R03 自动化出口不等于全环境发布验收。
