# G0012 / R06 实施结果

日期：2026-09-08。状态：已实施／完整本地门禁通过，部分原生交互待验收；未发布。验证归档：17:10（Asia/Shanghai）。

已交付普通格式转换、显式递归整理后打包、解压／整理结果清单打包，以及三个上下文入口。来源密码与目标秘密隔离，RAR5 认证限制传递，整项预算累计、每 ZIP 提交前回读、取消排空与清理残留报告已覆盖。

专项：[方案](implementation.md)、[契约](conversion-contract.md)、[保留矩阵](preservation-matrix.md)、[交互](interaction-design.md)、[验收](acceptance-matrix.md)。

## 本地验证

最终执行 `./tools/verify-local.ps1`，八项门禁全部通过。没有新增依赖或修改锁文件；没有新增 CI 配置，也没有执行正式插件打包。

| 检查 | 实际结果 |
| --- | --- |
| locked restore | 通过 |
| Debug 全解决方案 `-warnaserror` | 零警告、零错误 |
| Debug 引用资产求值 | Headless、两个引擎及许可证的声明和实际文件均通过 |
| Headless 全量测试 | 332 项通过，零失败、零跳过；新增 62 项 |
| Plugin/UI 全量测试 | 91 项通过，零失败、零跳过；新增 14 项 |
| 格式只读检查 | `dotnet format --verify-no-changes --no-restore` 通过 |
| Markdown 路径检查 | 86 份文档通过；归档完成后再检查 |
| 差异空白 | `git diff --check` 通过 |

合计 **423 项全部通过，新增 76 项**。覆盖 R06-AC01–08，并保留原解压、创建、加密、浏览、整理与窗口生命周期全部回归。测试发现和补齐的特殊对象拒绝适用于共享解压／浏览提取，不只在转换入口规避。

环境：Windows `10.0.26200` x64，.NET SDK `10.0.302`，Avalonia `12.1.0`，Plugin SDK `3.3.0`，SharpCompress `0.50.4`，SharpZipLib `1.4.2`。仅为当前开发环境证据，不代表全平台验收。

验证基线为 `eaa256101ef6604c8490577980de650e72d96180`，包含其后的工作区改动。134 个源码、测试、工具与配置文件清单 SHA-256 为 `301e6c11d93dc534424e35652dd962a7e063921482bac7d07becfb55e0b6ccbb`。范围、算法、命令和各步骤计时见[验证摘要](verification-summary.json)；日志、TRX、资产求值与源码摘要清单保留在 `artifacts/G0012/local-verification/`。历史 G0011 证据没有改写。

## 内容、资源与界面

12 个真实 RAR／7z 变体与公开原文逐路径、SHA-256 对照；包括固实、加密头、加密正文和 RAR5 受限认证。另有内嵌 ZIP 字节相等、空文件／空目录／空包、深度 1/2/3 多分支和来源 A／目标 B 验证。错误写入器产生遗漏或同长度篡改内容时，提交前真实回读拒绝。

两来源、两层、PDF 整理样本生成 **2,002 文件、2,006 个 ZIP 条目**，逐路径摘要独立一致。累计展开 **460,047 字节**、ZIP 暂存增长 **364,610 字节**、整项累计写入 **824,657 字节**；中间目录全部清理。样本在测试中生成并清理，可按测试复现，未把临时样本冒充长期保留产物。

最终复合样本耗时约 **12,882.55 ms**；测量区间累计托管分配 **729,708,808 字节**，测试进程峰值工作集 **181,886,976 字节**。样本放入已有不并行的 Performance 集合，避免其他并行用例污染本次分配统计。累计分配不是瞬时占用，进程峰值也不是该函数独占内存；原解压与写包缓冲、源复验和目标回读都包含在实测流程中，不当作裸 ZIP 编码速度。原始数据位于 `artifacts/performance/G0012/repack.json`，归档亦保留副本。

20 张渲染图位于 `artifacts/ui/G0012/`，覆盖深浅主题、640×520／900×760、准备、选项、完成、结果预览及来源变化。实际查看了窄窗口选项／预览、深色窄窗口错误和浅色大窗口完成图。首要错误显示在固定状态区，执行完成后收起选项，主要操作和“打开 ZIP 所在目录”按钮可见；命令绑定、Tab 移动、分页、取消／同步父 Scope 关闭及迟到回调有自动断言。

## 范围与遗留

普通转换使用暂存，不宣称零临时磁盘或完全无损；整理后直接以映射打包，没有额外的整理目录复制。目标只支持 ZIP；没有原地替换、删除来源、修复、任意流程引擎或断点恢复。中间内容默认清理，本期不提供保留选项。

结果打包只接受明确的解压／整理提交清单。浏览内承载的真实解压页可使用本期入口，单独浏览选择提取结果没有新增直接入口。页面仍是原三个顶级 Document，加父页面拥有的上下文子任务；没有第四个顶级 Document、Tool、Workflow 或历史持久化。

原生目录选择、系统打开目录、辅助功能、125%/150%/200% DPI 和真实 Host 仍待实机验收，Headless 渲染不替代这些检查。没有使用 AIFLOW，未执行 Windows CI、Release、正式插件包、部署或发布门禁。
