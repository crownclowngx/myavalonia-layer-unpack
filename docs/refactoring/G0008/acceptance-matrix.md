# G0008 / R02 验收矩阵

日期：2026-09-07。以下为真实测试映射，最终运行数量和整体状态见[结果](result.md)。

| R02 场景 | 证据来源 | 本地范围 |
| --- | --- | --- |
| AC01 文件夹合包 | PackTests 中文空文件空目录；PackDocumentTests 默认操作 | 真实 ZIP、路径与 SHA-256、默认页面闭环 |
| AC02 中文、空、小文件、长路径 | PackTests 中文、零字节、2,001 小文件；性能 JSON | 当前 Windows 实际样本，不承诺全 Zip64 或所有元数据 |
| AC03 重叠与同名 | PackTests 根排序编号；PackDocumentTests 首次映射展示 | 不重复、不覆盖，重要映射先呈现 |
| AC04 源目录内输出 | PackTests 精确排除与非法新子目录 | 源内已有目录支持；新增子目录需先创建，范围收缩明确 |
| AC05 同名与并发 | PackTests 并发外部输出 | 已有目标保持，两个新包独立提交 |
| AC06 源变化、占用、输出错误 | PackTests 摘要／清单／独占／阻塞路径／注入故障 | 拒绝伪成功，未提交文件回滚 |
| AC07 取消、关闭 | 真实写入与收尾取消；PackDocumentTests 排空及迟到回调 | 成功产物保留、暂存清理、终态可信 |
| AC08 互操作 | SharpZipLib TestArchive(true)、SHA-256、UnpackService 回读 | 独立读取器通过；原生外部工具人工未执行 |
| AC09 两类任务共存 | CompositionTests、PackDocumentTests、WindowLifecycleTests | 注册、独立 Scope、并行操作和关闭隔离 |

## 补充门禁与边界

保留原有全部解压回归；新增源字节／条目／深度／输出／时间预算、链接拒绝、普通输入与输出验证。所有代码变更通过统一 Debug 本地门禁后，才记录“本地验证通过”。

UI 检查包含主题、窄窗口、默认参数、键盘、操作绑定、结果位置、双任务和实际窗口代码。操作系统原生选择器／拖放／多 DPI 未验收，真实 Host、ALC、正式资产包、Release、安装与发布明确未执行。

源内新增输出目录策略与原路线图的宽泛目标相比采取保守限制；它在开始前明确提示，不产生额外目录或不完整 ZIP。R03 的分别打包与加密尚未开始。
