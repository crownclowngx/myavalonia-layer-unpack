# G0013 / R07 验收矩阵

| 场景 | 实现与测试证据 | 状态 |
| --- | --- | --- |
| R07-AC01 读取多于创建 | 统一独立矩阵；菜单仅 ZIP/TAR/TAR.GZ；浏览契约投影；真实 GUI 绑定 | 已实现，本地验证 |
| R07-AC02 新格式创建 | TAR/PAX、TAR.GZ；中文长路径、空目录、空文件、逐项摘要；libarchive 互操作 | TAR/TAR.GZ 通过；7z 不通过、未开放 |
| R07-AC03 无效组合 | TAR/TAR.GZ 拒绝密码、不适用偏好、错误扩展名；GUI 隐藏无效项，切换清密 | 已实现，本地验证 |
| R07-AC04 可列目录但正文损坏 | ZIP 正文等长损坏目录可读而完整检查失败；GZip 截断 1/4/8 字节尾部拒绝 | 已实现，本地验证 |
| R07-AC05 密码／缺卷／编码 | 上游真实密码样本、明确卷号、7z 缺卷或截断歧义、严格 UTF-8、下一步与秘密序列化 | 已实现，本地验证 |
| R07-AC06 校验受限 | RAR5 加密 MAC 不生成 CRC/认证证据；TAR 无正文校验和；XZ 容器算法不猜测 | 已实现，本地验证 |
| R07-AC07 资源和取消 | 64 MiB 内容及 2,000 条目样本中取消、2,000 条目完整检查、连续十次调用、预算不退款、超时和残留阻断 | 本地通过，实测数据已归档 |
| R07-AC08 默认 ZIP/解压回归 | 两个项目全量测试，默认 ZIP 与原 R01–R06 行为保留；转换目标明确仍为 ZIP | 477 项全通过、零跳过；最终记录见结果 |
| 生命周期与交互 | 父关闭排空、主动取消、禁止返回、迟到回调、秘密隔离；深浅主题窄窗口和键盘 | 自动验证，原生/DPI 遗留 |
| 发布 | Windows CI、Release、正式包、部署与 Host 发布验收 | 用户明确不执行 |

单元测试入口：`ArchiveCheckTests`、`ArchiveCheckSafetyTests`、`FormatCreationTests`、`SevenZipCandidateTests`、`ArchiveCheckDocumentTests`。候选负向测试通过只说明产品保持关闭，绝不表示 7z 创建能力通过。

命令、测试数量、性能、实际图片检查与未验收项见[结果](result.md)。
